const convertToBTC = () => {
  // const { balance, currencyId } = item;
  return 0;
};

const convertToEUR = () => {
  // Pass => item: Object

  return 0;
};

export default {
  convertToBTC,
  convertToEUR,
};
